import {EnversRevtypePipe} from './envers-revtype.pipe';

describe('EnversRevtypePipe', () => {
  it('create an instance', () => {
    const pipe = new EnversRevtypePipe();
    expect(pipe).toBeTruthy();
  });
});
